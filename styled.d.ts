import "styled-components";

declare interface DefaultTheme {
  title: string;
  colors: {
    primary: string;
    background: string;
    heading: string;
    text: string;
    lighter: string;
  };
}

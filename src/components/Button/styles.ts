import styled from "styled-components";

export const Container = styled.button`
  height: 3rem;
  padding: 0 2rem;
  cursor: pointer;
  background-image: linear-gradient(
    259.94deg,
    #7000ff 11.96%,
    rgba(255, 255, 255, 0.5) 314.88%
  );
  box-shadow: 0px 8px 24px rgba(112, 0, 255, 0.5);
  border-radius: 8px;
  color: #FFF;
  font-size: 1rem;
  font-weight: 500;
`;
